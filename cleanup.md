# Cleanup Candidates — Manual Review Required

> Generated by full-solution audit. **Nothing in this file has been deleted.** Walk each item, verify in the running app where applicable, then act.

Each entry includes the file path, evidence, and a recommended action. Group A items are confirmed dead from grep; Group B items need a manual UI walkthrough; Group C is intentional duplication left here for awareness; Group D is logic-level duplication to consolidate.

---

## A. Confirmed dead code (no inbound references outside their own files / tests)

| # | File | Type | Evidence | Recommended action |
|---|------|------|----------|--------------------|
| A1 | [apps/web/src/hooks/useScrollAnimation.ts](apps/web/src/hooks/useScrollAnimation.ts) | Unused hook (~25 LOC) | `grep useScrollAnimation` returns only the file itself. Zero importers. | Delete |
| A2a | [apps/web/src/components/shield/ShieldHeroStrip.tsx](apps/web/src/components/shield/ShieldHeroStrip.tsx) | Unused component (~94 LOC) | Only referenced by [tests/web/src/components/shield/ShieldHeroStrip.test.tsx](tests/web/src/components/shield/ShieldHeroStrip.test.tsx). No app importer. `ShieldHeroCarousel` is the live equivalent on `MarketplacePage`. | Delete component **and its test** |
| A2b | [apps/mobile/src/components/shield/ShieldHeroStrip.tsx](apps/mobile/src/components/shield/ShieldHeroStrip.tsx) | Unused component (mobile mirror) | No importer in mobile codebase. | Delete |
| A3a | [apps/web/src/components/shield/ShieldTile.tsx](apps/web/src/components/shield/ShieldTile.tsx) | Unused component (~70 LOC) | Only referenced by [tests/web/src/components/shield/ShieldTile.test.tsx](tests/web/src/components/shield/ShieldTile.test.tsx). `ShieldMetricsCard` is the live equivalent. | Delete component **and its test** |
| A3b | [apps/mobile/src/components/shield/ShieldTile.tsx](apps/mobile/src/components/shield/ShieldTile.tsx) | Unused component (mobile mirror) | No importer in mobile codebase. | Delete |
| A4 | [services/api/app/services/payment.py](services/api/app/services/payment.py) | Dead service module (~57 LOC) | `create_order()` and `verify_payment_signature()` have **zero callers**. Razorpay HMAC verification is implemented inline at [services/api/app/routers/investments.py:200-207](services/api/app/routers/investments.py#L200-L207) and again in `routers/webhooks.py`. | **Choose one:** (a) delete `payment.py`, OR (b) refactor the two router sites to call into it (preferred — see D1 below) |
| A5 | [docs/SHIELD_IMPLEMENTATION.md](docs/SHIELD_IMPLEMENTATION.md) | Doc may reference now-removed components | Mentions `ShieldHeroStrip` / `ShieldTile`. | After A2/A3 deletions, scrub or update this doc |

---

## B. UI popups / modals — verify in the running web app

Every modal below has at least one importer (i.e. it is **not dead by static analysis**), but the user has flagged that some popups appear redundant when using the app. Walk each trigger in the running web app and confirm the modal still serves a clear purpose. If a modal opens unconditionally, blocks the user without value, or duplicates another (e.g. two "complete profile" prompts back-to-back), file a follow-up to remove or consolidate.

| Modal | Imported by | Trigger to exercise | Watch for |
|-------|-------------|----------------------|-----------|
| [PersonaSelectionModal](apps/web/src/components/PersonaSelectionModal.tsx) | `App.tsx` | First login after signup | Fires only once per user; check it doesn't re-fire after persona is set |
| [VaultProfilingModal](apps/web/src/components/VaultProfilingModal.tsx) | `VaultsPage.tsx` | "Find my match" CTA | Overlaps in intent with `ProfilingGateModal` — confirm both are needed |
| [ProfilingGateModal](apps/web/src/components/ProfilingGateModal.tsx) | `OpportunityDetailPage.tsx` | Click-through on opportunity when profile is incomplete | If user hits this AND `VaultProfilingModal` in same session, consolidate |
| [VaultPickerModal](apps/web/src/components/VaultPickerModal.tsx) | `ProfileIndicator` | Header vault switcher | Confirm switcher is reachable on all relevant pages |
| [VaultComingSoonOverlay](apps/web/src/components/VaultComingSoonOverlay.tsx) | `PortfolioPage`, `MarketplacePage`, `VaultsPage` | Vault flagged `comingSoon` | If no vault has the flag set in prod, this overlay is effectively dead — consider removing the flag wiring |
| [CompanyOnboardingModal](apps/web/src/components/CompanyOnboardingModal.tsx) | `BuilderListingNewPage`, `BuilderListingEditPage` | Builder tries to list before completing company info | Verify it doesn't re-prompt after the first completion |
| [CreateOpportunityModal](apps/web/src/components/CreateOpportunityModal.tsx) | `VaultsPage`, `Navbar` | Two entry points (CTA + nav button) | Decide if both entry points are needed or one is legacy |
| [ExpressInterestModal](apps/web/src/components/ExpressInterestModal.tsx) | `OpportunityDetailPage` | "Express Interest" CTA | Confirm submission still hits `/eoi` endpoint cleanly |
| [ShareModal](apps/web/src/components/share/ShareModal.tsx) | `OpportunityDetailPage` | Share button | Verify referral link is generated server-side, not client-faked |
| [ShieldInfoModal](apps/web/src/components/shield/ShieldInfoModal.tsx) | `ShieldHeroCarousel`, `ShieldHeroStrip` (dead per A2) | "Learn more" on Shield section | After A2 deletion, this collapses to a single importer — that's fine |
| [CommunitySubtypeModal](apps/web/src/components/CommunitySubtypeModal.tsx) | `VaultsPage`, `BuilderListingNewPage` | Both flows | Confirm both flows still actually open it |
| [AddressDialog](apps/web/src/components/AddressDialog.tsx) | `BuilderListingNewPage`, `BuilderListingEditPage` | Address picker on listing forms | Picker UX redundancy with browser autofill? |

---

## C. Intentional duplication (do NOT delete — left here for awareness)

These exist twice (web + mobile) because of React vs React Native framework split. Keep both, but track drift. If divergence becomes painful, consider extracting shared *logic* (props shape, formatters, hooks) into a new `packages/wealthspot-ui-logic` package while leaving the rendering in each platform.

- `MatchScoreBadge` — [apps/web/src/components/profiling/MatchScoreBadge.tsx](apps/web/src/components/profiling/MatchScoreBadge.tsx) ↔ [apps/mobile/src/components/MatchScoreBadge.tsx](apps/mobile/src/components/MatchScoreBadge.tsx)
- `Badge`, `PropertyCard`, `MetricCard`, `ShieldSection`, `ShieldHeroCarousel`, `ShieldDot` — same web ↔ mobile pattern
- `useVaultConfig`, `useVaultFeatures` — exist in both [apps/web/src/hooks/](apps/web/src/hooks/) and [apps/mobile/src/hooks/](apps/mobile/src/hooks/); both consumed

---

## D. Inline-duplicated logic to consolidate (refactor, don't delete)

| # | Where | Duplication | Suggested fix |
|---|-------|-------------|----------------|
| D1 | [services/api/app/routers/investments.py](services/api/app/routers/investments.py) + [services/api/app/routers/webhooks.py](services/api/app/routers/webhooks.py) | Razorpay HMAC-SHA256 signature verification implemented inline in both | Refactor both sites to call `services/payment.py:verify_payment_signature()` (resolves A4 by giving the service callers) |
| D2 | [apps/web/src/lib/api.ts](apps/web/src/lib/api.ts) + [apps/mobile/src/lib/api.ts](apps/mobile/src/lib/api.ts) | snake↔camel transform, axios setup, 401-refresh queue, base URL handling — duplicated across both clients | Extract a new `packages/wealthspot-api-client` package; inject token storage via an adapter (localStorage for web, Expo SecureStore for mobile). Side benefit: brings missing `X-Diag-Id` request tracing to mobile. |

---

## Suggested execution order

1. **Quick wins** — delete A1, A2a/b, A3a/b and their tests; update A5. One PR. Risk: near-zero.
2. **A4 + D1 together** — refactor the two routers to call `services/payment.py`. One PR with a regression test.
3. **B audit** — open the running web app and walk each modal. File a follow-up issue per redundant popup.
4. **D2** — schedule as its own larger refactor (new shared package, two app migrations). Track separately.

After step 1 PR merges, run:
- `npm run lint`
- `npm run build:web`
- `cd services/api && pytest`
- Manual smoke on `MarketplacePage` (Shield section still renders via `ShieldHeroCarousel` / `ShieldMetricsCard`)
